!!!
%html
  %head
    %meta{ charset: "utf-8" }
    %meta{ content: "IE=edge,chrome=1", http: { equiv: "X-UA-Compatible" } }
    %meta{ name: "viewport", content: "width=device-width, initial-scale=1" }
    %title= current_page.data.title || "Cronfu"
    = stylesheet_link_tag "application"
    = favicon_tag 'assets/images/fav.png'
    = partial "partials/ga.html" if build?

  %body{ class: page_classes }
    %header
      %nav
        %p.logo
          cronfu
          %span by jimmy thai

        -# %ul
        -#   %li= nav_link "Article", "#"

    %main
      = yield

    %footer
      .container
        Jimmy Thai &copy; #{Date.today.year}

    = javascript_include_tag "https://code.jquery.com/jquery-2.1.1.min.js", "application"
